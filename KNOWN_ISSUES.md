# 既知の制約・今後の課題

## 現在の仕様上の制約

- 査定対象は土地のみ。
- 戸建、マンション、収益物件には未対応。
- 地域相場マスターは未実装。
- 過去査定データ管理は未実装。
- 複数物件管理は未実装。
- CSV入出力は未実装。
- PDF、Word、査定書出力は未実装。
- 外部APIは未使用。
- AIによる価格判断は行わない。
- 入力データは各端末・各ブラウザのlocalStorageに保存される。
- 端末間で自動共有されない。
- ブラウザデータ削除、端末故障、機種変更でデータを失う可能性がある。
- JSONバックアップを定期的に取得する必要がある。
- GitHub Pages版とローカル版の入力内容は共有されない。
- `file://`、`localhost`、GitHub Pagesなど、開くURLが異なる場合はブラウザ上の保存領域も分かれる。
- 現在は1物件分のみ保存する構造。
- 査定価格は概算であり、売却価格や成約価格、鑑定評価額を保証しない。
- 価格査定の計算には地域相場、取引事例、市場動向は自動反映されない。
- 算出根拠文は定型文生成であり、法的・価格的な妥当性を自動判断するものではない。
- `valuationEngine.js` が読み込めない場合、価格査定計算は利用できない。

## データ上の注意

- localStorageキーは `jusetsuResearchToolV1`。
- 現在のschemaVersionは2。
- 保存データの主要構造は `property`、`items`、`valuation`。
- `property`、`items`、`valuation` の構造を不用意に変更しない。
- チェック項目IDを変更すると既存状態を引き継げない可能性がある。
- 派生計算値より入力値を正として再計算する。
- 保存形式変更時は旧データ移行処理が必要。
- `valuation` の派生計算値がない旧schemaVersion 2データでも、入力値から再計算できる前提にする。
- `schemaVersion` がないデータと `schemaVersion: 1` は現行コードで読み込み互換対象。
- 未対応の将来schemaVersionは読み込まない。
- JSONインポートは現在の入力内容を上書きするため、実行前に確認が必要。

## 破損データ時の注意

- 起動時にlocalStorageのJSONが破損していた場合、初期画面が表示される。
- 起動直後には破損データは自動削除されない。
- その後に入力、エクスポート、インポート、リセット等を行うと上書きまたは削除される可能性がある。
- 救出が必要な場合は操作前にlocalStorageの値を退避する必要がある。
- 壊れたJSONや不正なバックアップJSONをインポートしようとした場合、現行コードは現在データを維持する設計。

## ドキュメント上の注意

- README.mdには過去段階の説明が残っている箇所がある。
- 最新状態を確認する場合は、README.mdの後半にあるPhase 1C-2の記載、現行コード、CHANGELOG.mdを優先して確認する。
- GitHub Pagesで実際に公開中かどうかは、現在のローカルコードだけでは判定できない。

## 今後の検討候補

- 査定履歴。
- 地域相場マスター。
- 過去査定からの初期単価提案。
- 中央値・平均値による参考単価提案。
- CSVインポート・エクスポート。
- 複数物件管理。
- 端末間共有。
- クラウド保存。
- 戸建査定。
- マンション査定。
- 収益物件査定。
- 査定書出力。
- バックアップ復元前の差分確認。